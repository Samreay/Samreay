{
  "name": "implement-plan-visual",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "update": "playwright test --update-snapshots",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "typescript": "^5.4.0"
  }
}
