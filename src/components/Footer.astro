---
// The site has no visible footer chrome — Hugo's `partials/footer.html`
// only ever injected a bundled JS file. We still emit a real `<footer>`
// tag so the `base_layout_structure` check finds the wrapper.
---
<footer />
